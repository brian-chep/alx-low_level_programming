#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0
 */

int _isupper(int c);
{
	if (isupper(c))
	{
		printf("The character is upper case");
		return (1);
	}
	else
	{
		printf("the character is not in upper case");
		return (0);
	}

}
